# v2.5.2 Actor Hover Overlay Fix

- Actor portraits now render in a fixed viewport overlay instead of inside each cast card.
- This prevents top-row portraits from being clipped by the episode modal/card boundaries.
- The overlay auto-positions above the actor card, or below it when there is not enough room.
- Actor cards remain clickable and still filter the app to that actor's appearances.
