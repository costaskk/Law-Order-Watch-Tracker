# Wolf Universe v2.4.1 Hero/Grid Fix

This fixes the oversized Up Next card by overriding an older desktop rule that still applied to `.dashboard .hero` and made the hero span four grid rows.

Updated file:

- `law_order_tracker_app/styles.css`

No JavaScript changes are required.
