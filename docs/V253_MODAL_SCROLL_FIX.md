# v2.5.3 Modal Scroll Fix

- Locks the page behind the episode popup while it is open.
- Restores proper scrolling inside the episode details modal.
- Makes the Cast section independently scrollable when there are many actors.
- Keeps actor portrait hover previews as fixed viewport overlays so they are not clipped.
- Hides actor hover previews while scrolling to avoid stale floating images.
